apiVersion: apps/v1
kind: Deployment

metadata:
  name: server-deployment
  namespace: cab-booking

spec:
  replicas: 2

  selector:
    matchLabels:
      app: server

  template:
    metadata:
      labels:
        app: server

    spec:
      serviceAccountName: server-sa

      containers:
        - name: server
          image: itsmeveee/cab-booking-server:latest
          imagePullPolicy: Always

          ports:
            - containerPort: 5005

          envFrom:
            - configMapRef:
                name: server-config
            - secretRef:
                name: server-secret

          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"

            limits:
              cpu: "500m"
              memory: "512Mi"

          livenessProbe:
            httpGet:
              path: /api/health
              port: 5005
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3

          readinessProbe:
            httpGet:
              path: /api/health
              port: 5005
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3