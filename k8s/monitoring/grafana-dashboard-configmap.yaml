apiVersion: v1
kind: ConfigMap

metadata:
  name: cab-booking-dashboard
  namespace: monitoring

  labels:
    grafana_dashboard: "1"

data:
  cab-booking-dashboard.json: |
    {
      "title": "Cab Booking Dashboard",
      "timezone": "browser",
      "schemaVersion": 39,
      "version": 1,
      "refresh": "10s",
      "panels": []
    }